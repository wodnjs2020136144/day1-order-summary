{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "1장 실습 · 계층 분리 (AI 없이 · 키 불필요)",
      "request": "launch",
      "mainClass": "com.skala.lab0.LabApplication",
      "projectName": "01_간식추천_3계층",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
